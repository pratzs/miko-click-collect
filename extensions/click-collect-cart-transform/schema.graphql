# Cart Transform API schema (subset)
scalar Decimal
scalar ID

type Query {
  cart: Cart!
}

type Cart {
  lines: [CartLine!]!
}

type CartLine {
  id: ID!
  attribute(key: String!): Attribute
}

type Attribute {
  key: String!
  value: String
}
