query RunInput {
  cart {
    pickupMethod: attribute(key: "miko_pickup_method") {
      value
    }
    locationName: attribute(key: "miko_location_name") {
      value
    }
    deliveryGroups {
      deliveryOptions {
        handle
        title
      }
    }
  }
}
